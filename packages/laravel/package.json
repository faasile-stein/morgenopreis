{
  "name": "@traveltomorrow/laravel",
  "version": "1.0.0",
  "private": true,
  "description": "Laravel CMS for SEO content management",
  "scripts": {
    "dev": "php artisan serve",
    "build": "composer install --optimize-autoloader --no-dev && php artisan config:cache && php artisan route:cache && php artisan view:cache",
    "test": "php artisan test",
    "lint": "./vendor/bin/pint",
    "format": "./vendor/bin/pint",
    "analyse": "./vendor/bin/phpstan analyse",
    "fresh": "php artisan migrate:fresh --seed",
    "sitemap": "php artisan sitemap:generate"
  },
  "keywords": [
    "laravel",
    "cms",
    "seo",
    "filament"
  ]
}
